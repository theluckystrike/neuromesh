# M08 — AppleScript CLI Full Automation Audit

```json
{
  "capabilities": {
    "open_terminal_and_run_commands": {
      "possible": true,
      "example_code": "tell application \"Terminal\"\n  activate\n  do script \"cd ~/projects && npm run build\"\nend tell",
      "notes": "Can run any shell command, capture output via 'do script' with result"
    },
    "interact_with_browsers_for_oauth_flows": {
      "possible": true,
      "example_code": "tell application \"Safari\"\n  activate\n  set URL of front window to \"https://github.com/login/oauth/authorize?client_id=xxx&redirect_uri=yyy\"\n  delay 3\n  do JavaScript \"document.getElementById('login_field').value = 'user'; document.getElementById('password').value = 'pass'; document.querySelector('input[type=submit]').click()\" in front window\nend tell",
      "notes": "Requires Accessibility permissions; Safari/Chrome supported; fragile to UI changes"
    },
    "fill_web_forms": {
      "possible": true,
      "example_code": "tell application \"System Events\"\n  keystroke \"John\"\n  keystroke tab\n  keystroke \"Doe\"\n  keystroke return\nend tell",
      "notes": "Works via UI element hierarchy or keystroke simulation; unreliable for dynamic forms"
    },
    "manage_git_operations": {
      "possible": true,
      "example_code": "tell application \"Terminal\"\n  do script \"cd /path/to/repo && git add . && git commit -m 'auto commit' && git push\"\nend tell",
      "notes": "Fully functional; can parse git output for error handling"
    },
    "publish_to_npm_pypi": {
      "possible": true,
      "example_code": "tell application \"Terminal\"\n  do script \"cd /path/to/package && npm publish\"\nend tell",
      "notes": "Requires npm token configured; same for PyPI with twine"
    },
    "manage_stripe_dashboard": {
      "possible": true,
      "example_code": "tell application \"Safari\"\n  set URL of front window to \"https://dashboard.stripe.com/payments\"\n  delay 5\n  do JavaScript \"document.querySelector('[data-testid=export-button]').click()\" in front window\nend tell",
      "notes": "Stripe API preferred; AppleScript only for UI automation of export tasks"
    },
    "monitor_logs": {
      "possible": true,
      "example_code": "set logFile to \"/var/log/system.log\"\nset logContent to do shell script \"tail -n 50 \" & logFile\nif logContent contains \"error\" then\n  display notification \"Error detected\"\nend if",
      "notes": "Can tail logs, grep patterns, trigger actions"
    },
    "send_notifications": {
      "possible": true,
      "example_code": "display notification \"Task completed\" with title \"Automation\" sound name \"default\"",
      "notes": "Native macOS notifications; also can use osascript for remote notifications"
    }
  },
  "limits": {
    "24_7_autonomous_pipeline": {
      "possible": false,
      "reason": "AppleScript requires user session; macOS sleep/idle stops scripts; no background daemon support without launchd",
      "workaround": "Use launchd plist to run scripts at intervals, but still requires logged-in user"
    },
    "shortcuts_app_integration": {
      "possible": true,
      "limitations": "Shortcuts can run AppleScript via 'Run AppleScript' action, but cannot run unattended without user interaction for some triggers",
      "example_shortcut_trigger": "Time of day, SSH connection, file change"
    },
    "reliability": {
      "ui_changes": "Fragile - any UI update breaks scripts",
      "permissions": "Requires Accessibility and Automation permissions",
      "error_handling": "Limited - no try-catch in AppleScript for UI elements"
    }
  },
  "implementation_complexity": "medium",
  "capital_required": 0,
  "estimated_monthly_revenue": 0,
  "real_platforms": ["macOS 15 Sequoia", "Safari 18", "Chrome 125", "Terminal", "Shortcuts 10"],
  "real_apis": ["osascript", "launchd", "System Events", "do shell script"],
  "note": "For production 24/7 pipelines, use CLI tools (bash, Python) with cron/launchd instead of AppleScript"
}
```