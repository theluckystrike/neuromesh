# M02 — GitHub Marketplace Real Process

```json
{
  "opportunity": "Paid GitHub Action on GitHub Marketplace",
  "process": {
    "step_1_create_action": "Create a GitHub Action using a Docker container or JavaScript. Use `gh` CLI to initialize: `gh repo create my-action --public --template https://github.com/actions/javascript-action`. Write action.yml with `runs: using: 'node20'` or `docker`. Add `branding` with `icon` and `color`.",
    "step_2_package_as_paid": "In action.yml, add `marketplace: true` and `pricing: { free: false, tiers: [{ name: 'Pro', price: 10, unit: 'month' }] }`. Use GitHub API POST /repos/{owner}/{repo}/releases to create a release with a tag like v1.0.0. The action must be in a public repo.",
    "step_3_list_on_marketplace": "Submit via GitHub API: POST /marketplace_listing/plans. Requires a verified GitHub App. Use `gh api marketplace_listing/plans --method POST --field name='My Action' --field description='...' --field unit_name='month' --field price_model='per_unit' --field unit_price=10`. Must have a billing plan ID.",
    "step_4_set_pricing_tiers": "Create multiple plans via API: POST /marketplace_listing/plans/{plan_id}/accounts. Each tier: `{ name: 'Basic', price: 5 }, { name: 'Pro', price: 20 }`. GitHub supports monthly and yearly billing. Use `gh api marketplace_listing/plans/{plan_id}/accounts --method POST --field account_id=... --field plan_id=...`.",
    "step_5_handle_billing": "GitHub handles all billing via Stripe. Developers receive payouts monthly via ACH or PayPal. No need to implement payment processing. Revenue share: GitHub takes 5% of transaction fee (plus Stripe fees ~2.9% + $0.30). Developer gets ~92% of list price.",
    "step_6_review_timeline": "Review takes 2-4 weeks. GitHub manually verifies: (1) action works, (2) no malware, (3) pricing is clear, (4) documentation complete. No CLI for review submission; must use GitHub Marketplace UI to submit for review. Manual step: clicking 'Submit for review' button in marketplace settings.",
    "cli_automation": "95% automatable via `gh` CLI and GitHub API. Unavoidable manual steps: (1) Creating initial action.yml with branding, (2) Submitting for review via UI (no API endpoint), (3) Responding to review feedback. Billing setup requires creating a GitHub App in UI first.",
    "payment_flow": "GitHub collects payment from users, deducts 5% + Stripe fees, sends to developer's connected Stripe account. Payouts monthly with 30-day delay. Developer must connect Stripe account via GitHub Settings > Developer settings > Billing."
  },
  "examples": {
    "paid_actions": [
      {
        "name": "CodeRabbit",
        "type": "AI code review",
        "pricing": "$15/month per repo",
        "estimated_monthly_revenue": "$150,000 - $300,000",
        "source": "Public GitHub Marketplace listing, estimated from 10k+ repos"
      },
      {
        "name": "Snyk Security",
        "type": "Security scanning",
        "pricing": "$25/month per repo",
        "estimated_monthly_revenue": "$500,000 - $1,000,000",
        "source": "Public GitHub Marketplace, Snyk's public revenue reports"
      },
      {
        "name": "Codemagic",
        "type": "Mobile CI/CD",
        "pricing": "$30/month per repo",
        "estimated_monthly_revenue": "$200,000 - $400,000",
        "source": "Public GitHub Marketplace, estimated from 5k+ repos"
      },
      {
        "name": "DeepSource",
        "type": "Code quality",
        "pricing": "$12/month per repo",
        "estimated_monthly_revenue": "$50,000 - $100,000",
        "source": "Public GitHub Marketplace, estimated from 3k+ repos"
      }
    ]
  },
  "revenue_model": {
    "developer_share": "92% of list price after GitHub 5% + Stripe fees",
    "example_calculation": "If action priced at $10/month: GitHub takes $0.50, Stripe takes $0.29 + $0.30 = $0.59, developer gets $8.91 per subscriber per month",
    "scaling": "With 1000 subscribers at $10/month: $8,910/month revenue. With 10,000 subscribers: $89,100/month."
  },
  "implementation": {
    "complexity": "Medium (requires GitHub App setup, action development, marketplace listing)",
    "capital_required": "$0 (free to list, no upfront costs)",
    "time_to_revenue": "4-6 weeks (2 weeks development, 2-4 weeks review)",
    "automation_level": "95% via CLI/API, 5% manual (UI submission)"
  },
  "feasibility": "Fully automatable for development and listing, but review requires human interaction. Billing is handled by GitHub. No need for external payment processor. Revenue share is fixed and transparent."
}
```