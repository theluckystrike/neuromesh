# M35 — macOS Security Restrictions for Automation

```json
{
  "research_topic": "macOS security restrictions blocking CLI automation in 2026",
  "date": "2026-05-22",
  "blockers": [
    {
      "name": "Gatekeeper",
      "description": "Validates code signing and notarization for executables. Blocks unsigned or unnotarized CLI tools from running without user override via right-click 'Open' or `spctl --master-disable` (requires admin).",
      "impact_on_automation": "Any CLI tool not signed with Apple Developer ID and notarized will fail to execute via launchd, cron, or direct shell invocation. Automated deployment of unsigned binaries is blocked.",
      "manual_intervention_required": true,
      "can_be_automated": false,
      "mitigation": "Sign and notarize all binaries; use `codesign` and `xcrun notarytool` in CI/CD pipeline."
    },
    {
      "name": "System Integrity Protection (SIP)",
      "description": "Restricts write access to /System, /usr, /bin, /sbin, and certain system daemons. Prevents modification of protected directories even with root.",
      "impact_on_automation": "Cannot install CLI tools into /usr/local/bin if SIP is enabled (default). Must use /opt/homebrew/bin or user-writable paths. Affects launchd plists in /System/Library/LaunchDaemons.",
      "manual_intervention_required": true,
      "can_be_automated": false,
      "mitigation": "Install tools in /usr/local (Homebrew) or /opt; avoid protected paths. SIP cannot be disabled without rebooting into Recovery Mode (manual)."
    },
    {
      "name": "Transparency, Consent, and Control (TCC)",
      "description": "Controls access to sensitive resources: camera, microphone, location, files (Desktop, Documents, Downloads), accessibility, input monitoring, full disk access, automation, and network volumes.",
      "impact_on_automation": "CLI tools need TCC permissions for file access, accessibility (GUI automation), and automation (controlling other apps). TCC prompts block headless automation. Permissions are per-app (bundle ID) and persist across launches.",
      "manual_intervention_required": true",
      "can_be_automated": false",
      "specific_permissions_needed": [
        {
          "permission": "Full Disk Access",
          "affected_tools": "Any CLI accessing files outside sandbox (e.g., rsync, grep on protected folders)",
          "manual_grant": true,
          "automation_possible": false,
          "note": "Must be granted via System Settings > Privacy & Security > Full Disk Access. No API to programmatically grant."
        },
        {
          "permission": "Accessibility",
          "affected_tools": "CLI tools using AppleScript or CGEventPost to automate UI (e.g., osascript, cliclick, keyboard maestro CLI)",
          "manual_grant": true,
          "automation_possible": false,
          "note": "Requires granting in System Settings > Privacy & Security > Accessibility. No programmatic grant."
        },
        {
          "permission": "Input Monitoring",
          "affected_tools": "CLI tools that listen to keyboard events (e.g., karabiner CLI, keylogger tools)",
          "manual_grant": true,
          "automation_possible": false,
          "note": "Separate from Accessibility; required for key event capture."
        },
        {
          "permission": "Automation",
          "affected_tools": "CLI tools that control other apps via Apple Events (e.g., osascript controlling Finder, Terminal, Safari)",
          "manual_grant": true,
          "automation_possible": false,
          "note": "Prompt appears on first use; must be accepted per app pair."
        },
        {
          "permission": "Files and Folders",
          "affected_tools": "CLI tools accessing Desktop, Documents, Downloads, iCloud, network volumes",
          "manual_grant": true,
          "automation_possible": false,
          "note": "Requires granting via System Settings > Privacy & Security > Files and Folders. Can be pre-granted via MDM."
        }
      ]
    },
    {
      "name": "Sandbox restrictions (App Sandbox)",
      "description": "If a CLI tool is sandboxed (e.g., distributed via Mac App Store), it has restricted file system, network, and hardware access. Most CLI tools are not sandboxed by default.",
      "impact_on_automation": "Sandboxed tools cannot access arbitrary files or network without entitlements. Not common for CLI automation tools.",
      "manual_intervention_required": false,
      "can_be_automated": true,
      "mitigation": "Avoid sandboxing; distribute outside App Store."
    },
    {
      "name": "AppleScript and Terminal permission dialogs",
      "description": "AppleScript can control Terminal.app without explicit permission if the script is run from Terminal itself (same process). However, controlling other apps (e.g., Finder, Safari) triggers TCC automation prompt.",
      "impact_on_automation": "`osascript -e 'tell application \"Terminal\" to do script \"ls\"'` works without dialog because Terminal controls itself. But `tell application \"Finder\"` triggers prompt on first use.",
      "manual_intervention_required": true",
      "can_be_automated": false,
      "mitigation": "Pre-grant Automation permission for Terminal to control target apps via MDM or manual acceptance."
    },
    {
      "name": "launchd agent network access",
      "description": "Launchd agents (user-level) have full network access by default. No restrictions unless sandboxed or restricted by firewall (e.g., Little Snitch).",
      "impact_on_automation": "No network blocker for launchd agents. They can freely connect to APIs, databases, etc.",
      "manual_intervention_required": false,
      "can_be_automated": true,
      "mitigation": "None needed."
    },
    {
      "name": "macOS Sequoia (2024+) security changes",
      "description": "Introduced stricter runtime protections: hardened runtime by default for notarized apps, mandatory code signing for all executables, and enhanced TCC enforcement for screen recording and accessibility. Also, new 'Private Access Tokens' for network privacy.",
      "impact_on_automation": "All CLI tools must be signed and notarized. Screen recording permission now required for any tool capturing display (e.g., screenshot CLI). TCC prompts cannot be suppressed programmatically.",
      "manual_intervention_required": true",
      "can_be_automated": false,
      "specific_changes": [
        "Hardened runtime required for all notarized apps (entitlements like com.apple.security.cs.disable-library-validation may be needed for code injection).",
        "Screen recording permission now enforced for CGDisplayStream, CGWindowListCreateImage, etc.",
        "Private Access Tokens may affect network calls that require user identity (e.g., Apple services)."
      ]
    },
    {
      "name": "Full Disk Access effect on CLI tools",
      "description": "CLI tools without Full Disk Access cannot read files in protected locations: ~/Library, ~/Desktop, ~/Documents, ~/Downloads, iCloud, Time Machine, and other users' home directories.",
      "impact_on_automation": "Automated backup, search, or file processing tools will fail silently or return partial results. `find`, `grep`, `rsync` on these paths return permission denied.",
      "manual_intervention_required": true",
      "can_be_automated": false,
      "mitigation": "Grant Full Disk Access via System Settings > Privacy & Security > Full Disk Access. Can be pre-configured via MDM (e.g., Jamf, Kandji) but not via CLI."
    },
    {
      "name": "Permissions that can be automated (via MDM or configuration profiles)",
      "description": "Some TCC permissions can be pre-approved using Mobile Device Management (MDM) configuration profiles. This includes: Full Disk Access, Accessibility, Automation, Files and Folders, Input Monitoring, Screen Recording.",
      "impact_on_automation": "If the macOS is enrolled in MDM (e.g., corporate device), these permissions can be silently granted without user interaction. For personal devices, manual grant is required.",
      "manual_intervention_required": false",
      "can_be_automated": true,
      "limitation": "Requires MDM enrollment and a configuration profile. Not available for unmanaged devices."
    },
    {
      "name": "Permissions that cannot be automated under any circumstances",
      "description": "No API or CLI exists to programmatically grant TCC permissions. Even with root or sudo, TCC database is protected by SIP and cannot be modified directly (except via `tccutil` which only resets, not grants). MDM profiles are the only automation path.",
      "impact_on_automation": "Any automation requiring a new permission on a fresh macOS install will require a human to click 'Allow' in a dialog.",
      "manual_intervention_required": true",
      "can_be_automated": false,
      "list": [
        "Camera",
        "Microphone",
        "Location",
        "Bluetooth",
        "Contacts",
        "Calendars",
        "Reminders",
        "Photos",
        "Speech Recognition"
      ]
    }
  ],
  "summary": "macOS 2026 security restrictions significantly hinder fully autonomous CLI automation. Gatekeeper, SIP, and TCC require manual intervention for first-time setup and permission grants. The only automation-friendly path is MDM-enrolled devices with pre-configured TCC profiles. For unmanaged devices, a human must grant Full Disk Access, Accessibility, Automation, and Input Monitoring permissions. Network access via launchd is unrestricted. AppleScript can control Terminal without dialogs but controlling other apps triggers TCC prompts. macOS Sequoia hardened runtime and screen recording enforcement add further friction. To achieve zero-touch automation, use signed/notarized binaries, install in user-writable paths, and leverage MDM for TCC grants."
}
```