# M37 — Full Automation Feasibility Score

```json
{
  "scores": {
    "npm_publish": {
      "score": 95,
      "manual_steps": [
        "2FA token entry if not using automation tokens (can use granular access tokens with no 2FA)",
        "Package name verification (must be unique, requires manual check if name taken)"
      ]
    },
    "pypi_twine": {
      "score": 90,
      "manual_steps": [
        "API token generation (one-time manual step via PyPI account settings)",
        "Project name registration (must be manually created on PyPI first for new packages)"
      ]
    },
    "github_action_listing": {
      "score": 85,
      "manual_steps": [
        "Creating marketplace listing metadata (action.yml must be manually formatted with correct schema)",
        "Initial repository setup (requires manual creation of public repo with specific naming convention)",
        "Marketplace badge approval (GitHub requires manual review for featured actions)"
      ]
    },
    "vs_code_vsce_publish": {
      "score": 70,
      "manual_steps": [
        "Azure DevOps PAT creation (one-time manual step in Azure portal)",
        "Extension manifest validation (package.json requires manual verification of publisher ID)",
        "Marketplace listing description (must be manually written and formatted)",
        "Icon and screenshot upload (requires manual image preparation)"
      ]
    },
    "rapidapi_listing": {
      "score": 60,
      "manual_steps": [
        "Provider account creation (requires manual email verification and KYC)",
        "API endpoint documentation (must be manually written in RapidAPI portal)",
        "Pricing plan setup (manual configuration of tiered pricing)",
        "Rate limiting configuration (manual setup in dashboard)"
      ]
    },
    "stripe_setup": {
      "score": 50,
      "manual_steps": [
        "Account registration (requires manual KYC verification with ID documents)",
        "Bank account linking (manual entry of routing/account numbers)",
        "Webhook endpoint configuration (manual setup in Stripe dashboard)",
        "Compliance form filling (tax info, business type - manual)"
      ]
    },
    "homebrew_tap": {
      "score": 80,
      "manual_steps": [
        "Tap repository creation (requires manual GitHub repo creation with specific name)",
        "Formula file writing (must be manually written in Ruby DSL)",
        "Formula testing (requires manual `brew audit` and `brew test` verification)"
      ]
    },
    "chrome_web_store": {
      "score": 40,
      "manual_steps": [
        "Developer account registration ($5 one-time fee, requires manual payment and verification)",
        "Extension packaging (manual zip creation with specific manifest format)",
        "Store listing creation (manual description, screenshots, promo images)",
        "Review process (manual approval by Google team, can take days)"
      ]
    }
  },
  "most_automatable_stack": {
    "stack": "npm publish + GitHub Action listing + Homebrew tap",
    "reasoning": "npm publish achieves 95% automation with granular access tokens. GitHub Action listing at 85% requires only one-time metadata setup. Homebrew tap at 80% needs initial formula creation but is fully scriptable thereafter. Combined, this stack covers package distribution (npm), CI/CD marketplace (GitHub Actions), and CLI tool distribution (Homebrew) with minimal manual intervention.",
    "estimated_monthly_revenue": "$500-$2000",
    "implementation_complexity": "Medium",
    "capital_required": "$0 (all platforms free for open source)"
  }
}
```