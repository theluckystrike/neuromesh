# M33 — Stripe Webhooks Without Server

```json
{
  "options": [
    {
      "name": "Stripe CLI listen locally via launchd",
      "description": "Use Stripe CLI to forward webhooks to a local script, kept alive 24/7 via macOS launchd. Works as long as Mac is on and internet is stable.",
      "setup": {
        "steps": [
          "Install Stripe CLI: brew install stripe/stripe-cli/stripe",
          "Create a local script (e.g., /usr/local/bin/stripe-webhook-handler.sh) that runs a simple HTTP server (e.g., using Python or Node) on a port like 3000.",
          "Set up launchd plist at ~/Library/LaunchAgents/com.stripe.webhook.plist with content: { 'Label': 'com.stripe.webhook', 'ProgramArguments': ['/usr/local/bin/stripe', 'listen', '--forward-to', 'http://localhost:3000/webhook'], 'RunAtLoad': true, 'KeepAlive': true }",
          "Load the plist: launchctl load ~/Library/LaunchAgents/com.stripe.webhook.plist"
        ],
        "costs": {
          "monthly": "$0 (Stripe CLI free, local machine electricity cost negligible)",
          "setup": "$0"
        },
        "reliability": "Medium - depends on Mac staying on, no downtime, and internet connectivity. Not suitable for production due to local machine risks."
      }
    },
    {
      "name": "Cloudflare Workers (free tier)",
      "description": "Use Cloudflare Workers to receive webhooks and process them serverlessly. Free tier includes 100k requests/day, 10ms CPU time per request.",
      "setup": {
        "steps": [
          "Sign up for Cloudflare account (free).",
          "Create a Worker using wrangler CLI: npm install -g wrangler, wrangler init stripe-webhook-worker",
          "Write worker code (JavaScript) to parse Stripe webhook signature using crypto, verify event, and forward to your backend or process inline.",
          "Deploy: wrangler publish",
          "Set Stripe webhook endpoint to https://your-worker.your-subdomain.workers.dev/webhook",
          "Add environment variables for Stripe secret key and endpoint secret."
        ],
        "costs": {
          "monthly": "$0 (free tier: 100k requests/day, 1 million requests/month, 10ms CPU time)",
          "setup": "$0",
          "overage": "$0.30 per million requests beyond free tier"
        },
        "reliability": "High - Cloudflare global network, 99.99% uptime SLA, auto-scaling."
      }
    },
    {
      "name": "AWS Lambda",
      "description": "Use AWS Lambda with API Gateway to receive Stripe webhooks. Free tier includes 1 million requests/month and 400,000 GB-seconds.",
      "setup": {
        "steps": [
          "Create AWS account (free tier eligible for 12 months).",
          "Create a Lambda function (Python or Node.js) that validates Stripe signature using stripe library, processes event.",
          "Set up API Gateway HTTP API with a POST route /webhook, integrate with Lambda.",
          "Deploy API and set Stripe webhook endpoint to the API URL.",
          "Add environment variables for Stripe secret key and endpoint secret."
        ],
        "costs": {
          "monthly": "$0 (within free tier: 1M requests, 400k GB-seconds, 750 hours API Gateway)",
          "setup": "$0",
          "overage": "$0.20 per million requests beyond free tier, $0.0000166667 per GB-second"
        },
        "reliability": "Very High - AWS infrastructure, 99.99% uptime, but requires more setup and potential cold starts."
      }
    },
    {
      "name": "Vercel Functions",
      "description": "Use Vercel serverless functions to handle webhooks. Free tier includes 100 GB-hours, 100k requests/month.",
      "setup": {
        "steps": [
          "Create Vercel account (free tier).",
          "Create a Next.js or Node.js project with an API route (e.g., pages/api/webhook.js).",
          "Implement Stripe webhook verification using stripe.webhooks.constructEvent.",
          "Deploy to Vercel: vercel --prod",
          "Set Stripe webhook endpoint to https://your-project.vercel.app/api/webhook."
        ],
        "costs": {
          "monthly": "$0 (free tier: 100k requests, 100 GB-hours, 3000 build minutes)",
          "setup": "$0",
          "overage": "$20/month for 1M requests, $0.04 per additional GB-hour"
        },
        "reliability": "High - Vercel edge network, auto-scaling, but free tier limited to 100k requests/month."
      }
    },
    {
      "name": "ngrok",
      "description": "Use ngrok to expose a local server to the internet for webhook testing. Not suitable for production 24/7 due to uptime and cost.",
      "setup": {
        "steps": [
          "Install ngrok: brew install ngrok",
          "Run a local HTTP server on port 3000 (e.g., node server.js).",
          "Run ngrok: ngrok http 3000",
          "Set Stripe webhook endpoint to the ngrok URL (e.g., https://abc123.ngrok.io/webhook).",
          "For 24/7, need paid plan ($8.25/month) for reserved domains and uptime."
        ],
        "costs": {
          "monthly": "$8.25 (paid plan for reserved domain and uptime) or $0 (free but URL changes each restart, limited to 40 connections/min)",
          "setup": "$0"
        },
        "reliability": "Low - free tier URL changes, limited bandwidth; paid plan more reliable but still depends on local machine."
      }
    }
  ],
  "recommendation": {
    "cheapest": "Cloudflare Workers (free tier) - $0/month, high reliability, easy setup.",
    "most_reliable": "AWS Lambda (within free tier) - $0/month, enterprise-grade uptime, but more complex.",
    "for_solo_developer": "Cloudflare Workers is best balance of cost, reliability, and simplicity. Stripe CLI via launchd is viable for development but not production."
  }
}
```