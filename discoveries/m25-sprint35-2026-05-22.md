# M25 — AppleScript Browser Automation for OAuth

```json
{
  "query": "Can AppleScript fully automate browser-based OAuth flows on macOS in 2026?",
  "answer": {
    "capability": "Partially, with significant limitations and security risks. AppleScript can automate UI interactions (clicks, keystrokes) in Safari/Chrome, but it cannot reliably handle dynamic page loads, pop-ups, or 2FA. For OAuth flows, CLI-based approaches with localhost redirect are far superior.",
    "exact_appleScript_code": {
      "github_oauth": {
        "description": "Automates GitHub OAuth authorization page in Safari. Assumes user is already logged into GitHub.",
        "code": "tell application \"Safari\"\n  activate\n  set URL of front document to \"https://github.com/login/oauth/authorize?client_id=YOUR_CLIENT_ID&redirect_uri=urn:ietf:wg:oauth:2.0:oob&scope=repo&response_type=code\"\n  delay 3\n  -- Click the 'Authorize' button (may fail if page hasn't loaded)\n  tell front document\n    do JavaScript \"document.querySelector('button[name=\"authorize\"]').click()\"\n  end tell\n  delay 2\n  -- Capture the authorization code from the page\n  set authCode to do JavaScript \"document.querySelector('code').innerText\"\n  return authCode\nend tell",
        "limitations": "Requires Safari automation enabled, may break if GitHub changes UI, cannot handle 2FA, assumes no popups."
      },
      "stripe_dashboard_login": {
        "description": "Automates Stripe Dashboard login in Chrome. Requires credentials stored in Keychain (not hardcoded).",
        "code": "tell application \"Google Chrome\"\n  activate\n  set URL of active tab of front window to \"https://dashboard.stripe.com/login\"\n  delay 3\n  -- Fill email (retrieved from Keychain via shell script)\n  set email to do shell script \"security find-generic-password -a 'stripe_email' -w\"\n  tell application \"System Events\"\n    keystroke email\n    keystroke tab\n    -- Fill password\n    set password to do shell script \"security find-generic-password -a 'stripe_password' -w\"\n    keystroke password\n    keystroke return\n  end tell\n  delay 5\n  -- 2FA will break this flow\nend tell",
        "limitations": "2FA required for Stripe, cannot be bypassed with AppleScript alone. Security risk: password exposure in memory."
      },
      "npm_login": {
        "description": "Automates npm login via browser (npm uses OAuth with GitHub).",
        "code": "tell application \"Safari\"\n  activate\n  set URL of front document to \"https://www.npmjs.com/login\"\n  delay 3\n  -- Click 'Sign in with GitHub'\n  tell front document\n    do JavaScript \"document.querySelector('button[data-testid=\"github-login-button\"]').click()\"\n  end tell\n  delay 5\n  -- Assume GitHub already logged in; click authorize\n  tell front document\n    do JavaScript \"document.querySelector('button[name=\"authorize\"]').click()\"\n  end tell\n  delay 3\n  -- Capture redirect URL (npm redirects to localhost:8080, which AppleScript cannot capture)\nend tell",
        "limitations": "npm uses localhost redirect, which AppleScript cannot intercept. Flow fails."
      }
    },
    "2FA_handling": {
      "totp_appleScript": "AppleScript can generate TOTP via shell script using `oathtool` (if installed), but cannot automatically input it into a browser without UI scripting. Example:\n  set totp to do shell script \"oathtool --totp -b YOUR_BASE32_SECRET\"\n  tell application \"System Events\" to keystroke totp\n  -- Security risk: secret stored in plaintext in script",
      "limitations": "TOTP secret must be stored in script or Keychain. Keychain access requires user permission. 2FA still breaks automation if the page dynamically loads the TOTP field."
    },
    "security_implications": [
      "AppleScript can read/write to Keychain but requires user approval for each access (macOS 2026 security hardening).",
      "UI scripting exposes credentials to screen recording/monitoring tools.",
      "Hardcoded secrets in .applescript files are plaintext.",
      "Automated browser interactions can be detected as bot activity, triggering account locks.",
      "2FA secrets stored in scripts violate security best practices."
    ],
    "better_approach": {
      "method": "CLI-based OAuth with localhost redirect server",
      "description": "Use a local HTTP server to capture the OAuth redirect with the authorization code or token. No browser automation needed.",
      "implementation": {
        "github_cli": "gh auth login --web --hostname github.com\n# Or use device flow:\ngh auth login --hostname github.com --scopes repo",
        "stripe_cli": "stripe login --interactive\n# Opens browser but uses localhost redirect to capture token",
        "npm_cli": "npm login --auth-type=web\n# Opens browser, redirects to localhost:8080, npm CLI captures token",
        "custom_script": "#!/bin/bash\n# Start local server on port 8080\npython3 -m http.server 8080 &\nSERVER_PID=$!\n# Open OAuth URL\nopen \"https://github.com/login/oauth/authorize?client_id=...&redirect_uri=http://localhost:8080&response_type=code\"\n# Wait for callback (requires parsing server logs)\nsleep 30\nkill $SERVER_PID"
      },
      "advantages": [
        "No UI automation fragility",
        "Handles 2FA via browser (user completes 2FA manually, token captured automatically)",
        "No credential exposure in scripts",
        "Works with all OAuth providers that support localhost redirects"
      ],
      "estimated_monthly_revenue": "$0 (no direct revenue, but enables automated CI/CD pipelines worth $500-2000/month in saved developer time)",
      "implementation_complexity": "Low (1 hour to script)",
      "capital_required": "$0 (uses existing tools: gh, stripe, npm CLIs)"
    },
    "verdict": "AppleScript is NOT recommended for OAuth automation in 2026. Use CLI-based OAuth with localhost redirect servers for reliable, secure, and maintainable automation."
  }
}
```