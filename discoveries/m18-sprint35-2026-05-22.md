# M18 — launchd vs cron vs PM2 on macOS

```json
{
  "comparison": {
    "launchd": {
      "auto_restart_on_crash": "Yes, via 'KeepAlive' key with CrashOnly=true or always",
      "log_management": "Built-in stdout/stderr redirection via StandardOutPath/StandardErrorPath, log rotation via logrotate or newsyslog",
      "resource_monitoring": "Limited to basic CPU/memory limits via WatchPath, no built-in metrics; use third-party tools like osquery or top",
      "scheduling": "Yes, via StartCalendarInterval (cron-like) or WatchPaths, but less flexible than cron for complex schedules",
      "managing_multiple_agents": "Yes, via separate plist files per agent, but no centralized UI; use launchctl list/batch scripts",
      "best_for": "macOS native, crash recovery, low overhead, integration with system services"
    },
    "cron": {
      "auto_restart_on_crash": "No, must implement wrapper script with loop or use external process manager",
      "log_management": "Manual via cron job output redirection (>> /var/log/agent.log 2>&1), no built-in rotation",
      "resource_monitoring": "None, requires external tools like ps, top, or custom scripts",
      "scheduling": "Yes, best for periodic tasks with precise cron syntax, but no event-driven triggers",
      "managing_multiple_agents": "Yes, via separate crontab entries, but no dependency management or parallel control",
      "best_for": "Simple periodic tasks, low complexity, no crash recovery needed"
    },
    "pm2": {
      "auto_restart_on_crash": "Yes, built-in with max_restarts and restart_delay",
      "log_management": "Built-in log rotation via pm2-logrotate module, centralized logs in ~/.pm2/logs",
      "resource_monitoring": "Yes, via pm2 monit, pm2 status, and pm2-metrics for CPU/memory per process",
      "scheduling": "No native scheduling, but can combine with cron or launchd for triggers",
      "managing_multiple_agents": "Yes, via ecosystem.config.js with multiple apps, cluster mode, and graceful shutdown",
      "best_for": "Node.js processes, complex multi-agent setups, production-grade monitoring"
    }
  },
  "can_launchd_replace_pm2": "Partially. launchd can auto-restart and manage logs, but lacks PM2's built-in monitoring, cluster mode, zero-downtime reload, and centralized management. For simple Node.js agents, launchd is sufficient; for complex multi-process systems, PM2 is better.",
  "configurations": {
    "launchd_plist": {
      "filename": "com.user.node-agent.plist",
      "path": "~/Library/LaunchAgents/",
      "xml": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<!DOCTYPE plist PUBLIC \"-//Apple//DTD PLIST 1.0//EN\" \"http://www.apple.com/DTDs/PropertyList-1.0.dtd\">\n<plist version=\"1.0\">\n<dict>\n    <key>Label</key>\n    <string>com.user.node-agent</string>\n    <key>ProgramArguments</key>\n    <array>\n        <string>/usr/local/bin/node</string>\n        <string>/path/to/agent.js</string>\n    </array>\n    <key>WorkingDirectory</key>\n    <string>/path/to/agent</string>\n    <key>KeepAlive</key>\n    <dict>\n        <key>SuccessfulExit</key>\n        <false/>\n        <key>CrashOnly</key>\n        <true/>\n    </dict>\n    <key>RunAtLoad</key>\n    <true/>\n    <key>StandardOutPath</key>\n    <string>/tmp/agent-stdout.log</string>\n    <key>StandardErrorPath</key>\n    <string>/tmp/agent-stderr.log</string>\n    <key>EnvironmentVariables</key>\n    <dict>\n        <key>PATH</key>\n        <string>/usr/local/bin:/usr/bin:/bin</string>\n        <key>NODE_ENV</key>\n        <string>production</string>\n    </dict>\n</dict>\n</plist>"
    },
    "cron_monitor": {
      "entry": "*/5 * * * * /usr/local/bin/node /path/to/monitor.js >> /var/log/agent-monitor.log 2>&1",
      "monitor_script_example": "const { execSync } = require('child_process');\nconst agents = ['agent1', 'agent2'];\nagents.forEach(name => {\n  const pid = execSync(`pgrep -f ${name}`).toString().trim();\n  if (!pid) {\n    execSync(`launchctl start com.user.${name}`);\n    console.log(`${new Date().toISOString()} Restarted ${name}`);\n  }\n});"
    },
    "pm2_ecosystem": {
      "filename": "ecosystem.config.js",
      "content": "module.exports = {\n  apps: [\n    {\n      name: 'agent1',\n      script: '/path/to/agent1.js',\n      interpreter: '/usr/local/bin/node',\n      cwd: '/path/to/agent1',\n      env: { NODE_ENV: 'production' },\n      max_restarts: 10,\n      restart_delay: 5000,\n      log_date_format: 'YYYY-MM-DD HH:mm:ss',\n      error_file: '/var/log/pm2/agent1-error.log',\n      out_file: '/var/log/pm2/agent1-out.log',\n      merge_logs: true,\n      max_memory_restart: '500M',\n      autorestart: true\n    },\n    {\n      name: 'agent2',\n      script: '/path/to/agent2.js',\n      interpreter: '/usr/local/bin/node',\n      cwd: '/path/to/agent2',\n      env: { NODE_ENV: 'production' },\n      max_restarts: 10,\n      restart_delay: 5000,\n      log_date_format: 'YYYY-MM-DD HH:mm:ss',\n      error_file: '/var/log/pm2/agent2-error.log',\n      out_file: '/var/log/pm2/agent2-out.log',\n      merge_logs: true,\n      max_memory_restart: '500M',\n      autorestart: true\n    }\n  ]\n};"
    }
  },
  "applescript_automation": {
    "best": "launchd",
    "reason": "AppleScript can directly control launchd via 'do shell script \"launchctl start/stop/load/unload\"' commands, enabling seamless integration with macOS automation workflows. PM2 requires shell commands but lacks native AppleScript support. Cron is not directly controllable from AppleScript without shell wrappers.",
    "example_applescript": "tell application \"System Events\"\n    do shell script \"launchctl load ~/Library/LaunchAgents/com.user.node-agent.plist\"\n    delay 2\n    do shell script \"launchctl start com.user.node-agent\"\nend tell"
  },
  "recommendation": {
    "best_overall": "PM2 for Node.js agents with complex requirements; launchd for simple, native macOS agents; cron for periodic monitoring tasks only",
    "hybrid_approach": "Use launchd to start PM2 at boot (via plist), then PM2 manages all Node.js agents. Use cron for external health checks and AppleScript for UI automation triggers."
  }
}
```